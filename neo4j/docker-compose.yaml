version: "3.9"

# Graph database. Browser UI at http://localhost:7474
# Default login: neo4j / my_password
services:
  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/my_password
    volumes:
      - ./neo4j-data:/data
